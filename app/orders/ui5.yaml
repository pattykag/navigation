specVersion: "2.5"
metadata:
  name: NS.orders
type: application
server:
  customMiddleware:
    - name: fiori-tools-servestatic
      afterMiddleware: compression
      configuration:
        paths:
          - path: /resources/ns.customers
            src: ../customers/webapp
          - path: /appconfig
            src: appconfig
